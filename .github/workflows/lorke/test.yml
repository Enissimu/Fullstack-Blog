name: Testing stuff

on:
  push:
    branches:
      - main

jobs:
  simple_deployment_pipeline:
    name: react dev pipe
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: "16"
      - name: folder context
        run: ls
